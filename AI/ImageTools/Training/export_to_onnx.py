"""Future placeholder for exporting the custom classifier to ONNX.

ImageTools EXE builds should eventually use an exported ONNX model, not a
training stack. Keep model files local unless they are explicitly requested.
"""


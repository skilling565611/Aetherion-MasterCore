[project]
name = "aetherion-ai-audio-tools"
version = "0.1.0"
description = "Local audio preparation tools for AI chatbot uploads."
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
